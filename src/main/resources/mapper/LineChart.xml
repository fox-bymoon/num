<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demo.dao.LineChart">
    <!-- 查询时间线-->
    <select id="findNumTime" parameterType="java.lang.String" resultType="java.util.Map">
         SELECT GROUP_CONCAT(a.time_h) as newtime FROM
	        (SELECT time_h as time_h FROM v_total where time_d = #{sysTime} and mid = #{mid} ORDER BY time_h) a
    </select>
    <!-- 查询良品-->
    <select id="findNumTime" parameterType="java.lang.String" resultType="java.util.Map">
        SELECT GROUP_CONCAT(a.good_number_total) as newtime FROM
	        (SELECT good_number_total,time_h as time_h FROM v_total where time_d = #{sysTime} and mid = #{mid} ORDER BY time_h) a

    </select>
    <!-- 查询不良品-->
    <select id="findNumTime" parameterType="java.lang.String" resultType="java.util.Map">
        SELECT GROUP_CONCAT(a.bad_number_total) as newtime FROM
	        (SELECT bad_number_total,time_h FROM v_total where time_d = #{sysTime} and mid = #{mid} ORDER BY time_h) a
    </select>
</mapper>